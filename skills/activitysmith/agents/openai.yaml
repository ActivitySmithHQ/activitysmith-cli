interface:
  display_name: "ActivitySmith"
  short_description: "Send push notifications and run full Live Activity lifecycle via ActivitySmith CLI"
  default_prompt: "Use ActivitySmith scripts to send push notifications (including optional channels/redirection/actions) and to run start/update/end live activity lifecycle with ACTIVITYSMITH_API_KEY auth."
